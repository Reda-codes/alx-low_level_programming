#include <stdio.h>
#include <ctype.h>
/* betty style doc for function main goes there */
/**
 * mul - multiplies two integers.
 * @a: first input
 * @b: second input
 * Return: returns the multiplies of the two inputs.
 */
int mul(int a, int b)
{
	return (a * b);
}
